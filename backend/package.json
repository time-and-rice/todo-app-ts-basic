{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "dist/api/index.js",
  "scripts": {
    "dev:server": "ts-node-dev --transpile-only --no-notify api/index.ts",
    "dev:docker": "docker-compose -p todo-app-ts-basic up",
    "build": "rm -rf dist && tsc",
    "format": "prettier -l \"./api/**/*.{js,ts,json}\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.10.2",
    "apollo-server": "^3.13.0",
    "graphql": "^16.8.1",
    "nexus": "^1.3.0"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "prisma": "^5.10.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
